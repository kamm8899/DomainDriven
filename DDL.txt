(    id   bigint not null        primary key,    text varchar(80));alter table message    owner to cs548user;create table sequence(    seq_name  varchar(50) not null        primary key,    seq_count numeric(38));alter table sequence    owner to cs548user;create table patient(    id        bigint       not null        primary key,    dob       date,    name      varchar(255),    patientid varchar(255) not null        unique);alter table patient    owner to cs548user;create table provider(    id         bigint       not null        primary key,    name       varchar(255),    npi        varchar(255),    providerid varchar(255) not null        unique);alter table provider    owner to cs548user;create index index_provider_providerid    on provider (providerid);create table treatment(    id                    bigint       not null        primary key,    dtype                 varchar(31),    diagnosis             varchar(255),    treatmentid           varchar(255) not null        unique,    patient_id            bigint        constraint fk_treatment_patient_id            references patient,    provider_id           bigint        constraint fk_treatment_provider_id            references provider,    dosage                double precision,    drug                  varchar(255),    enddate               date,    frequency             integer,    startdate             date,    dischargeinstructions varchar(255),    surgerydate           date,    treatmentdates        bytea);alter table treatment    owner to cs548user;create table treatment_treatment(    treatment_id          bigint not null        constraint fk_treatment_treatment_treatment_id            references treatment,    followuptreatments_id bigint not null        constraint fk_treatment_treatment_followuptreatments_id            references treatment,    primary key (treatment_id, followuptreatments_id));alter table treatment_treatment    owner to cs548user;create table physiotherapytreatment_treatmentdates(    physiotherapytreatment_id bigint        constraint physiotherapytreatment_treatmentdates_physiotherapytreatment_id            references treatment,    treatmentdates            date);alter table physiotherapytreatment_treatmentdates    owner to cs548user;create table radiologytreatment_treatmentdates(    radiologytreatment_id bigint        constraint fk_radiologytreatment_treatmentdates_radiologytreatment_id            references treatment,    treatmentdates        date);alter table radiologytreatment_treatmentdates    owner to cs548user;